# Errors

Log format: date, what happened, deterministic or infrastructure, conclusion (if any).

| Date | Error | Kind | Conclusion |
|---|---|---|---|
| 2026-09-03 | Raw SSDP multicast from a shell process: `OSError: [Errno 65] No route to host` | Deterministic | macOS Local Network privacy blocks multicast for non-entitled processes. Use Bonjour (`NWBrowser`) instead. |
| 2026-09-05 | app stuck on discovering with no connections after rebuilds | Deterministic | ad-hoc signature changes per build, so macOS's Local Network grant no longer matched; fixed by signing with a stable Apple Development identity |
| 2026-09-05 | `xcodebuild ... -allowProvisioningUpdates` fails: `error: No Accounts: Add a new account in Accounts settings.` / `error: No signing certificate "Mac Development" found...` under `CODE_SIGN_STYLE: Automatic` | Infrastructure | No Apple ID is signed into Xcode's Accounts preferences on this machine and no cached provisioning profile exists, so Automatic signing can't mint the Development profile the sandboxed app needs even though the certificate is present. **Superseded same day** — resolved without needing an account by switching to `CODE_SIGN_STYLE: Manual` with an explicit `CODE_SIGN_IDENTITY`/`DEVELOPMENT_TEAM`/`PROVISIONING_PROFILE_SPECIFIER: ""`; see the next two rows. |
| 2026-09-05 | Manual signing with `CODE_SIGN_IDENTITY: "Apple Development: hello@jenswedin.com (4B85FPKBH8)"` + `DEVELOPMENT_TEAM: 4B85FPKBH8` fails: `error: No certificate for team '4B85FPKBH8' matching 'Apple Development: hello@jenswedin.com (4B85FPKBH8)' found` | Deterministic | The parenthetical in the certificate's CN/display name (`4B85FPKBH8`) is the developer's personal ID, not the team ID — Apple's usual convention of CN-parenthetical == OU doesn't hold for this cert. The real team ID is the certificate's OU, `FDBYWW84AR` (confirmed via `openssl x509 -noout -subject` on the exported cert, and via `codesign -dvv` on a build that succeeded with the SHA-1 fingerprint as `CODE_SIGN_IDENTITY`). Fixed by setting `DEVELOPMENT_TEAM: FDBYWW84AR`; `codesign -dvv` then shows `TeamIdentifier=FDBYWW84AR` and `Authority=Apple Development: hello@jenswedin.com (4B85FPKBH8)`. No Xcode account or provisioning profile was needed once the team ID was correct. |
| 2026-09-05 | `xcodebuild test` against the freshly (manually-)signed app hung ~5–6 min then `** TEST FAILED **` / "the test runner hung before establishing connection" on its first run after a fresh signed build | Infrastructure | A retry of the identical `xcodebuild test` command immediately succeeded (15/15 tests, `** TEST SUCCEEDED **`). Looks like a one-off launch-handshake flake with a freshly-signed sandboxed `LSUIElement` test host rather than anything caused by the signing change itself — no pattern established yet since it hasn't recurred; log a new row if it happens again. |
| 2026-09-05 | `log show --predicate 'subsystem == "com.jenswedin.SonosRemote"'` returned zero lines right after launching the (correctly, stably) signed app, even though the app was actively discovering/connecting | Deterministic | macOS unified logging only persists `.notice`/`.error`/`.fault` to the log store by default; `.info`-level `Logger` calls (most of task 15b's new diagnostics) are ephemeral and only visible via a live `/usr/bin/log stream --level info ...` started before the activity, not a retroactive `log show`. `.error` lines from the same subsystem DID show up in `log show` once real request failures occurred, confirming this is a level-retention default, not a logging bug. Also: plain `log` in this zsh shell resolves to a builtin, not `/usr/bin/log` — invoking it unqualified fails with `too many arguments`; always use the full path. |
| 2026-09-05 | Launched (correctly signed) app's topology fetches to its chosen gateway player (192.168.1.105:1443) all failed with `NSURLErrorDomain Code=-1001 "The request timed out."` in a tight retry loop with growing backoff | Infrastructure | Confirmed independently of the app: `ping 192.168.1.105` (100% packet loss) and a direct `curl` to the same URL (5s timeout, `http_code=000`) both failed from the shell at the same time — the real Sonos player at that address was transiently unreachable on the network, unrelated to app code. The task 15b bootstrap-retry fix handled this exactly as designed: `.noPlayersFound` once the discovery timeout elapsed, then continued retrying with growing backoff (observed gaps ~9s, 11s, 12s, 18s, 27s between attempts) indefinitely in the background rather than giving up. |
