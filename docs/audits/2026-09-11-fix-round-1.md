# Audit fix round 1 — results

Base: `c2bee59` (main after the audits and the plan; metrics baseline `docs/audits/metrics/before.json`). Head at the closing metrics run: `a08b658` on branch `audit-fixes-1`. Plan: `docs/superpowers/plans/2026-09-11-audit-fixes-round-1.md`. Method: subagent-driven development, one implementer per task, a review per task (Tasks 5–8 reviewed by the controller because reviewer dispatches were blocked by the session's permission classifier), metrics recorded after every task with `scripts/audit-metrics.py`.

## Before / after

| metric | before | after | change | better? |
|---|---:|---:|---:|:--:|
| custom-styled buttons/links with no .focusable() | 11 | 0 | -11 | ✅ |
| .focusable() call sites in views | 2 | 17 | +15 | ✅ |
| icon buttons framed at 20–23 pt | 2 | 0 | -2 | ✅ |
| toggle glyphs whose on state is colour-only | 1 | 0 | -1 | ✅ |
| AccessibilityNotification call sites | 0 | 1 | +1 | ✅ |
| .updatesFrequently trait sites | 0 | 5 | +5 | ✅ |
| accessibilityAction sites | 0 | 2 | +2 | ✅ |
| accessibilityHint sites | 0 | 2 | +2 | ✅ |
| .secondary/.tertiary text colour sites | 32 | 0 | -32 | ✅ |
| .system(size:) without relativeTo | 13 | 13 | +0 |  |
| accessibilityReduceMotion honoured | 0 | 1 | +1 | ✅ |
| view files reading state.snapshot | 6 | 0 | -6 | ✅ |
| orderedGroups is a computed (re-sorting) property | 1 | 0 | -1 | ✅ |
| snapshot stream uses bufferingNewest | 0 | 1 | +1 | ✅ |
| Household.apply skips unchanged snapshots | 0 | 1 | +1 | ✅ |
| LazyVStack sites | 0 | 0 | +0 |  |
| websocket ping interval | 30 | 90 | +60 | ✅ |
| SMAppService queried in a @State initialiser | 1 | 0 | -1 | ✅ |
| preconditionFailure/fatalError/try! in SonosKit sources | 4 | 0 | -4 | ✅ |
| TrustStore pins the leaf key hash | 0 | 1 | +1 | ✅ |
| isLocalSpeakerAddress applied in discovery | 0 | 1 | +1 | ✅ |
| trust revoked when a player is lost | 0 | 1 | +1 | ✅ |
| PlaybackProgress.maximumMillis clamp exists | 0 | 1 | +1 | ✅ |
| privacy: .public log arguments | 26 | 16 | -10 | ✅ |
| Text literals interpolating metadata | 1 | 0 | -1 | ✅ |
| project.yml pins exact dependency versions | 0 | 1 | +1 | ✅ |
| lines of Swift in app + package sources | 3572 | 3944 | +372 |  |
| snapshots yielded for 40 identical events | 40 | 1 | -39 | ✅ |
| namespaces subscribed on the coordinator while the panel is closed | 6 | 2 | -4 | ✅ |
| playerVolume:1 subscribed at all | 1 | 0 | -1 | ✅ |
| SonosKit tests | 81 | 94 | +13 | ✅ |
| app tests | 41 | 44 | +3 | ✅ |
| warnings from our own files | 0 | 0 | +0 |  |

The dynamic rows (snapshot yields, subscriptions, test counts, warnings) come from the probe tests and the two suites; `-` in the history means that run skipped the tests.

## Findings addressed

| Task | Commits | Findings |
|---|---|---|
| 1 Keyboard reach, targets, non-colour cue | `1214cb2`, `bac5b3e` | a11y C1, S4, S5 |
| 2 Speaker key pinning, address validation | `a04e648`, `bbb0b6f` | security H-1, H-2, L-1, L-2 |
| 3 No traps on network data, clamped millis | `d10fa6c` | security H-3, H-4 |
| 4 Narrow observable state, buffered stream, stored ordering | `c0e7824` | performance H1, H3, M1 |
| 5 Announcements, live regions, row action, group hints | `b779765` | a11y S2, S3, S6, S7 |
| 6 Subscriptions only while the panel is open | `a75e898` | performance H5, H2 |
| 7 Contrast tokens, borders, disabled dimming | `2307cfd` | a11y S1, M10, m10 |
| 8 Quick wins | `b377eae` | security M-4, L-3 (transport and household logs), L-4; performance M5, M8, L2; a11y M5, M6 |

## Still open

Accessibility: M1 screen changes not announced to VoiceOver; M2 no list semantics on the rows; M3 fixed font sizes; M4 `lineLimit(1)` truncation without a hover value; M7 the inert "Custom" chip; M8 `RoomPicker` can show an empty selection; M9 inline errors clear after three seconds; m1 no hints beyond the room row; m2 uppercase display strings as accessible names; m3 favorite second line missing from the name; m4 arrow glyphs read aloud; m5 shuffle/repeat value string without trait (trait added in Task 1; value kept); m6 search field labelled by placeholder only; m7 hero playback state not exposed; m8 Loudness description placement; m9 tone slider values read as bare signed numbers; m11 favorites play glyph looks like a separate button.

Performance: H4 eager favorites list and artwork downloads; M2 favorites filter allocations per render; M3 full-resolution artwork decode with no cache; M4 four SOAP round trips per EQ read/write; M6 bootstrap refetch loop when favorites never succeed; M7 `URLSessionTransport` never invalidated on retry; M9 eager error descriptions; M10 volume slider task churn; M11 no network-path or wake monitoring; L1 zero-height first frame; L3 enumerated `ForEach`; L4 row focus binding may defeat diffing; L5 double sort per topology event; L6 linear snapshot lookups; L7 repeated `selectedGroup` reads; L8 one task per incoming volume event; L9 serial sub probes; L10 `PlayerSocket` retain cycle until `stop()`.

Security: M-1 topology payloads still widen the allow-list (now bounded by pinning); M-2 identifiers interpolated unescaped into REST paths; M-3 artwork URLs unrestricted; M-5 unbounded growth caps; M-6 no response-size limit; L-5 plaintext UPnP (protocol limitation); L-6 substring SOAP parsing; L-7 reconnect counter resets early; L-8 release script does not verify the tap remote. Partial: L-3 — `BonjourDiscovery.swift` and `PlayerSocket.swift` still log ids and addresses as `.public` (14 sites). Follow-ups noted in review: IPv6 addresses pass validation but the three URL builders do not bracket them (fail-closed today); a pin mismatch is logged but has no user-visible status; pins are keyed by address, so a DHCP change re-pins.

## Notes on the metrics history

For tasks 1, 3 and 7 the metrics JSON `commit` field records the parent commit, because the script ran before the code commit; tasks 2, 4, 5, 6 and 8 recorded metrics in a separate follow-up commit, so their field is exact. The values are correct in every row. `sec.address_validation` measures where the string `isLocalSpeakerAddress` appears (under `Discovery/`), not behaviour; the behavioural guarantee is in `TrustPolicyTests` and `DiscoveryTests`.

## Owner's manual checks

- System Settings → Keyboard → Keyboard navigation **off**: open the panel, press Space (play/pause toggles), Tab through header icons, transport, volume and rooms, Cmd-← / Cmd-→ skip tracks, Up/Down/Return/Space in the rooms list; reopen the panel on a sub-screen and confirm the Back button takes focus.
- VoiceOver: an unreachable speaker error is spoken; opening the panel while discovering speaks "Looking for Sonos"; the room row offers Play/Pause in the actions rotor; the Group screen's switch reads its membership and hint.
- Activity Monitor: with music playing and the panel closed, the app shows no once-per-second wakeups; reopening fills the hero and progress within about a second.
- Light mode: second lines and captions are clearly darker than before; the error line is readable; with Increase Contrast on, the search field and card borders thicken.
- Reduce Motion on: screen changes fade instead of sliding.
