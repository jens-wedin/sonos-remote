# Session memory

Last state: v0.1.0 of Sonos Remote complete per `docs/superpowers/plans/2026-09-05-sonos-remote.md`. All 20 tasks (including the Task 15b controller hotfix) are done on branch `sdd-v0.1.0`, which is awaiting merge to `main`. SonosKit's 69 package tests and the app's 16 tests pass. The manual checklist in `knowledge/procedural/manual-test-checklist.md` was written this session but has not yet been run — manual checklist pending — owner to run before tagging `v0.1.0`.

Next ideas (not started): media keys + Control Center now-playing, "show track in menu bar" option, AppKit panel shell for animated resize, Developer ID signing for sharing.
