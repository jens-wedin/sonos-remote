# Knowledge index

Read top-down; open only what you need.

- `domain/sonos-local-api.md` — how the speakers' local API behaves (ports, headers, message shapes, quirks).
- `domain/github-releases-api.md` — the GitHub `releases/latest` endpoint the update check uses: headers, rate limit, fields, where the code lives.
- `procedural/build-and-run.md` — exact commands to test the package, generate the project, build, run.
- `procedural/manual-test-checklist.md` — manual checklist against real speakers, run before tagging a release.
- `procedural/release.md` — one-time Developer ID and notarization setup, and how `scripts/release.sh` cuts a release (GitHub Release + Homebrew cask).
- `ERRORS.md` — error log with conclusions.
